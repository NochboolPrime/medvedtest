# Руководство по работе с Design Mode и темами

## Как работает система тем в Design Mode

Когда вы изменяете цвета в Design Mode (режиме дизайна), изменения автоматически применяются **только к текущей активной теме**, не затрагивая другую тему.

### Принцип работы

1. **ThemeDesignHelper** - специальный компонент, который отслеживает текущую активную тему
2. Когда вы меняете CSS-переменные через Design Mode, компонент перехватывает эти изменения
3. Изменения применяются только к селектору активной темы (`:root` для темной или `.light` для светлой)

### Структура CSS-переменных

#### Темная тема (по умолчанию)
```css
:root {
  --background: #1a1f2e;     /* Фон страницы - темно-синий */
  --foreground: #edf1f7;     /* Цвет текста - светло-серый */
  --card: #1a1f2e;           /* Фон карточек - темно-синий */
  --primary: #1e375c;        /* Основной цвет - синий */
  --accent: #b19d76;         /* Акцентный цвет - бежевый */
}
```

#### Светлая тема
```css
.light {
  --background: #ffffff;     /* Фон страницы - белый */
  --foreground: #1c2433;     /* Цвет текста - темно-синий */
  --card: #f8f9fa;           /* Фон карточек - светло-серый */
  --primary: #1e375c;        /* Основной цвет - синий */
  --accent: #b19d76;         /* Акцентный цвет - бежевый */
}
```

### Как изменять цвета в Design Mode

1. **Переключитесь на нужную тему** (светлая/темная) с помощью кнопки переключения тем в header
2. **Откройте Design Mode** и измените нужные цвета
3. **Изменения применятся только к активной теме**
   - Если вы на темной теме → изменения в `:root`
   - Если вы на светлой теме → изменения в `.light`

### Какие цвета можно менять

| Переменная | Где используется | Темная тема | Светлая тема |
|-----------|------------------|-------------|--------------|
| `--background` | Фон страницы, основных блоков | #1a1f2e | #ffffff |
| `--foreground` | Основной цвет текста | #edf1f7 | #1c2433 |
| `--card` | Фон карточек | #1a1f2e | #f8f9fa |
| `--card-foreground` | Текст на карточках | #edf1f7 | #1c2433 |
| `--primary` | Кнопки, ссылки, акценты | #1e375c | #1e375c |
| `--primary-foreground` | Текст на primary элементах | #ffffff | #ffffff |
| `--muted` | Второстепенные элементы | #1a1f2e | #f1f3f5 |
| `--muted-foreground` | Текст второстепенных элементов | #b7c5db | #6c757d |
| `--accent` | Акцентный цвет (бежевый) | #b19d76 | #b19d76 |
| `--border` | Границы элементов | rgba(183, 197, 219, 0.1) | rgba(28, 36, 51, 0.1) |

### Компоненты, использующие переменные темы

Все основные компоненты используют семантические токены:

- **Header** - `bg-background`, `text-foreground`
- **Hero** - `bg-background`, `text-foreground`
- **About** - `bg-muted`, `bg-card`, `text-foreground`
- **Services** - `bg-background`, `bg-card`
- **Equipment** - `bg-card`, `text-card-foreground`
- **Contact** - `bg-muted`, `bg-card`
- **Footer** - `bg-background`, `text-foreground`

### Проверка изменений

После изменения цветов в Design Mode:

1. Проверьте, что изменения видны в текущей теме
2. Переключите тему - другая тема должна остаться без изменений
3. Вернитесь к измененной теме - ваши изменения должны сохраниться

### Отладка

Компонент ThemeDesignHelper выводит в консоль информацию о применяемых изменениях:

```
 CSS-переменные изменены в design-режиме для темы: dark
 Применены изменения к теме: dark { --background: "#1a1f2e", ... }
```

Откройте консоль браузера (F12), чтобы видеть эти логи.

### Примеры изменений

**Пример 1: Изменить цвет фона в темной теме**
1. Убедитесь, что активна темная тема
2. В Design Mode измените `--background` на нужный цвет
3. Изменение применится только к темной теме

**Пример 2: Изменить цвет акцентов в светлой теме**
1. Переключитесь на светлую тему
2. В Design Mode измените `--accent` на нужный цвет
3. Изменение применится только к светлой теме
4. При переключении на темную тему - акцент останется прежним

### Важные замечания

- ⚠️ **Всегда проверяйте активную тему** перед изменением цветов
- 💡 Изменения сохраняются автоматически в соответствующий CSS-селектор
- 🔄 При переключении темы изменения сохраняются
- 📱 Система работает на всех устройствах и размерах экрана

### Технические детали

**ThemeDesignHelper** использует:
- `MutationObserver` для отслеживания изменений CSS
- `useTheme` hook из next-themes для определения активной темы
- `data-active-theme` атрибут для идентификации темы
- Автоматическое применение классов `.light` и `.dark` к `<html>`

Это обеспечивает корректную работу изменений цветов независимо для каждой темы.
